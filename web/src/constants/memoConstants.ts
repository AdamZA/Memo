export const DEFAULT_PAGE = 1;
export const DEFAULT_PAGE_LIMIT = 20;

export const DEBOUNCE_DELAY_MS = 400;

// 10 seconds was chosen as a balance between providing reasonably fresh data to users
// and minimizing server load from frequent refresh requests.
export const REFRESH_INTERVAL_MS = 10000;
